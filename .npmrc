# @gelabs/sp is a RESTRICTED (private) package in the 'gelabs' npm org. Installing
# it needs a read token — provide it via the NPM_TOKEN environment variable.
@gelabs:registry=https://registry.npmjs.org/
//registry.npmjs.org/:_authToken=${NPM_TOKEN}
