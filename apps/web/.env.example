# Base URL of the separate Fastify @sp/api. The Next app proxies /api + /auth to
# it (see next.config.ts rewrites). Same-origin from the browser → first-party
# session cookies, no CORS. Dev default is the local @sp/api on :8787.
NEXT_PUBLIC_API_BASE=http://localhost:8787
